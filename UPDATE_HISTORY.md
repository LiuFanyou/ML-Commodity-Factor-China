# 项目更新记录

本文记录研究流程的主要版本变化。历史版本用于解释现有文件来源；当前默认版本为版本 8。

## 版本 1： 验证基线

最初版本建立日频研究流水线：自动发现 Tushare CSV、筛选商品品种、因果选择主力合约、构造期限结构、生成 20 日因子序列、训练 1D-CNN，并按滚动训练窗口进行样本外测试。回测采用截面等权多空，并输出 1、3、5 bps 费用敏感性。

## 版本 2：本地运行与进度输出（不重要）

默认数据路径切换到本地项目目录，增加 `tqdm` 和阶段进度输出。数据扫描主动跳过 `fut_settle_by_date` 中的逐日结算参数小文件，只读取完整年度行情，提高本地调试效率。

## 版本 3：5 日标签与扩展窗口

预测目标从次日收益改为未来 5 个交易日累计超额收益，时间验证改为扩展窗口。模型使用不压缩 20 日时间维度的轻量卷积结构，组合加入 3 日 EMA 和持仓缓冲，以降低换手。

## 版本 4：2018—2025 数据瘦身、信号加权和 GMM 风控

数据加载曾限定为 2018—2025，首个训练窗为 2018—2021，2022 样本外测试。组合由等权改为预测强度加权。新增市场波动率、平均相关性和流动性压力三维状态特征，并逐测试年仅用历史数据拟合两状态 GMM。系统性压力概率高于 0.8 时，组合毛杠杆降至 50%。

## 版本 5：原始量价输入、Self-Attention 与多任务双头

模型输入一度使用 7 个原始日频特征。网络升级为 Conv1d 局部嵌入加单层 Transformer Encoder，并加入分类头和回归头。损失为 0.7 BCE 加 0.3 Huber，综合信号为上涨概率乘回归幅度。训练期按每 5 个交易日下采样，降低 5 日标签重叠。交易执行改为前后 30%且超过零阈值才入场，越过中位数后退出。

## 版本 6：旧工业级 59 因子交接包

项目接入 `ml_feature_registry.csv` 和 `ml_features_development.csv.gz`。`FACTOR_COLS` 从注册表动态生成，特征矩阵按 `trade_date + product` 与标签面板合并，模型输入改为 `[Batch, 59, 20]`，默认 dropout 提高至 0.40。

该版本对应的旧特征矩阵截至 2024-12-31，且品种池包含 `EG/FG/OI/RU`，缺少新研究池中的 `CS/L/SF/SN`。因此该交接包不再是当前默认数据源，只保留用于历史复现和兼容模式。

## 版本 7：整合合约日表成为默认训练数据

更新时间：2026-07-23。

新增并核验 `futures_contract_daily_30varieties_2015_2025.csv`。该表以 `trade_date + ts_code` 为唯一键，共 53 个字段，覆盖 2015—2025 年 30 个品种，包含合约元数据、OHLC、结算、成交量、持仓量、手续费、保证金、现货、仓单和库存。

主要变化如下：

1. `feature_mode` 新增 `auto / integrated / handoff`。默认 `auto` 在发现整合表时选择 `integrated`。
2. 行情加载优先只读取整合表，不再与年度 `fut_daily_raw.csv` 混读。
3. 数据和训练起点调整为 2015 年，首个样本外测试仍为 2022 年。
4. 从因果主力合约面板重建 59 个输入：45 个连续特征和 14 个缺失质量标志。
5. 特征覆盖动量、趋势效率、波动、偏度、振幅、量仓、Amihud、期限结构、现货、仓单和库存。
6. 连续特征按日做 MAD 稳健标准化和板块去均值；低频字段结合 `*_age_days` 屏蔽过度陈旧观测。
7. 修复 Dataset 四元组与训练循环三元组不一致的问题。
8. 修复模型已定义品种 Embedding、但训练与预测过程未传入 `product_id` 的问题。
9. 新增 `run_integrated_training.bat`，使用项目虚拟环境运行并记录 `integrated_training.log`。

旧年度行情、旧交接包、上游整合中间文件和旧输出均未删除。其状态见 `FILE_STATUS.md`。

## 版本 8：统一评价与审计展示层（当前版本，不重要）

更新时间：2026-07-23。

本次只增强结果展示，不改变任何因子定义、训练样本、模型结构、预测值、信号、持仓、费用公式或原有结果文件。

新增统一评价输出：

1. 横截面预测：每日 Pearson IC、Rank IC、分年 Rank IC。
2. 收益风险：累计收益、年化收益、年化波动、Sharpe、最大回撤、Calmar 和平均换手。
3. 费用情景：新增独立的 `0 / 3 / 5 / 10 bps` 表；原回测的 `1 / 3 / 5 bps` 保持不变。
4. 统计检验：Newey-West/Bartlett HAC 标准误、t 统计量和双侧 p 值；20 日圆形块自助法 95% 区间。
5. 稳健性：1/5/20 日持有期、能源/金属/化工/农产品、剔除每日最低 20% 流动性尾部、信号延迟一个观测市场交易日。
6. 分组展示：每日预测五分组收益及相邻分组单调性评分。
7. 方法审计：`evaluation_methodology.json` 明确记录 IC、IR、HAC、Bootstrap、持有期、板块、流动性和信号延迟口径。

新增评价层在原结果完成落盘后执行。若评价层出现特殊空样本或其他异常，只会发出警告并写入 `evaluation_audit_error.txt`，不会阻断模型、预测、持仓和原回测文件保存。

## 当前结果状态

项目中的 `futures_ml_outputs`、`run_report.md` 旧数值和 `run_report.pdf` 来源于整合数据流程完成前的历史运行。代码和数据已经更新，但新的 2015—2025 全量深度学习训练尚未在本次修改中完成。因此当前文档不会把旧绩效数值冒充为新整合数据结果；正式结论应以新一轮训练生成的 `unified_metrics_panel.csv`、`cost_scenarios.csv` 和其他审计表为准。
