先说结论：你的问题很扎实，但目前把几件**不同层级的事**混在一起了——尤其是「开发期验证 / 封存期末考 / 训练数据范围 / 防泄露」。下面先点出概念缺口，再逐题讲清。

---

## 你当前理解里缺什么（基于这 12 问）

| 缺口             | 表现                                            | 需要先建立的概念                                             |
| ---------------- | ----------------------------------------------- | ------------------------------------------------------------ |
| 时间角色混淆     | 问「2025 解封后用什么训练、和扩展窗口有何区别」 | **开发验证 ≠ 最终测试**；扩展窗口是反复练习卷，2025 是冻结后的期末考 |
| 研究 vs 实盘时间 | 问「标签未来走完才可知」                        | **回测能看见未来答案，实盘当天不能**；标签只用于事后评分     |
| 连续 vs 真实合约 | 问映射为何必要                                  | 连续序列是**研究用尺子**，真实合约是**能下单的标的**         |
| 点时安全 PIT     | 问 PIT / +1 日                                  | 信息**何时对交易者可见**，比信息本身更重要                   |
| 协议即法律       | 问拒绝覆盖、冻结意义                            | 规则先于结果；否则是数据挖掘                                 |
| 接口契约         | 问交接包如何保证对接                            | `ml_feature_registry` 是 **API 合同**，不是随便一张表        |
| OOS 术语         | 直接问含义                                      | Out-of-Sample：模型没用来拟合/挑选的样本                     |

---

# 1. 2025 封存的意义、解封、训练数据、与扩展窗口区别

### 封存意义
防止「边看 2025 边改因子 / 特征 / 模型」。  
若 2025 可反复窥视，你的「样本外」会变成**调参后的样本内**，论文结论不可信。

### 「真实运行时何时解封」
在本项目设计里：

- **开发阶段**：2025 永不进入 `training_handoff`，建模同学看不到  
- **解封时刻**：筛选口径、特征清单、执行规则、模型超参**全部冻结之后**，只允许**开一次** `sealed_evaluation`  
- 不是日历到了 2026 就自动解封，而是**研究流程上的一次最终考试**

### 测 2025 时用什么训练？
是的：用 **2015–2024 全部开发数据**训练最终模型（在已冻结的特征与协议下），再在 **2025** 上做一次预测与回测。

### 那和扩展窗口有何区别？（这是你最关键的混淆点）

两者回答的问题不同：

```text
扩展窗口（2019–2024 逐年）
├── 作用：开发期内部的“反复模拟样本外”
├── 每次：只用该折训练期末之前的数据训练/评价
├── 目的：筛因子、看稳健性、写 A–F 分类、做诊断
└── 可以看结果、写报告（但仍禁止用2025）

封存 2025
├── 作用：全流程冻结后的“期末考”
├── 训练：2015–2024 全体（规则已不再改）
├── 目的：估计真实推广能力，一次定论
└── 结果不得回头改上游规则
```

类比：

- 扩展窗口 ≈ 大学四年每学期模拟考（可改进学法）  
- 2025 封存 ≈ 毕业统考（试卷拆封前不得再改复习大纲）

所以：**不是「扩展窗口已经用过 2015–2023，封存又用 2015–2024」就重复了**；前者是过程控制，后者是最终声明。

---

# 2. OOS 是什么意思

**OOS = Out-of-Sample（样本外）**

某段数据**没有参与**该结论所依赖的拟合、筛选、调参，再用它评价，就叫 OOS。

本项目里：

- 对 fold_2023 而言，2023 年是该折的 OOS  
- 对整次筛选而言，2025 是最终 OOS  
- 若用 2025 的 Rank IC 去删因子再重报 2025，则 2025 **不再是真正 OOS**

---

# 3. 「输出目录若已存在则拒绝覆盖」+ 冻结规则意义

### 拒绝覆盖是什么意思
`run_pipeline.py` 发现 `outputs/`、`reports/`、`training_handoff/`、`sealed_evaluation/`、`features/` 已存在时**直接报错退出**，不覆盖旧结果。

### 为什么要这样
1. **可复现**：别人拿到的结果对应某一版代码+数据哈希（`RUN_MANIFEST.json`）  
2. **防误操作**：避免「不小心重跑」把封存前结果冲掉  
3. **审计痕迹**：要重跑必须显式归档/删除，人为确认

### 冻结规则的意义
科学实验里，**规则必须先于结果**。  
若先看 IC 再改成本、持有期、主力定义，得到的是被数据讨好的故事，不是可推广规律。  
冻结 = 把「玩法」写死，只允许换预测器 \(f(X)\)。

---

# 4. 主键唯一性、PIT、疑似涨跌停锁价代理

### 主键唯一性
研究单元是 `trade_date × product`（或合约表的 `trade_date × ts_code`）。  
同一天同一品种若有两行，程序不知道该信哪行 → **硬停**。  
意义：避免静默丢行/重复行导致虚假 IC。

### PIT（Point-in-Time）违规检查
问的是：**这条信息在信号日 T 是否已经对交易者可见？**

例如库存有 `stock_observation_date`。若观察日 ≥ 交易日，却被用来算 T 日因子，就是用了未来信息 → 记为 PIT violation。  
本项目基本面还强制 **观察日后延迟 1 个交易日** 才可用。

意义：防「上帝视角回测」（look-ahead bias）。

### 疑似涨跌停锁价代理
源数据没有交易所精确涨跌停板明细，于是用 OHLC 近似：开高低收几乎锁死等 → 标 `suspected_price_limit_lock`。  
标签构造时，进出场若疑似锁价，可排除该样本。

意义：避免把「根本买不进/卖不出」的日子当成可交易收益。

---

# 5. 连续主力 → 真实合约映射（详细）

### 为什么要映射
- **TuShare 连续主力**：一条长期价格序列，方便算动量、波动等，但代码往往是合成/连续标记，**不能直接下单**  
- **真实合约**（如 `RB2501`）：有真实开盘价，才能定义可交易收益与换月

若用连续价差当收益，会混进换月跳空伪收益。

### 具体怎么做（本仓库实现）

对每个 `trade_date × product`：

1. 取当日连续行情字段：`pre_close, pre_settle, open, high, low, close, settle`  
2. 与同日该品种**所有真实合约**对齐比较  
3. 每个字段「两边都有数且足够接近」得 1 分 → `mapping_score`  
4. 选最高分；若并列：优先延续昨日合约；否则按持仓量→成交量→更远到期等打平  
5. 禁止到期日「往回跳」的换月（`prohibit_maturity_backtracking`）  
6. 输出 `mapped_actual_ts_code` + 映射质量等级  

### 时间含义
- **T 收盘**：连续行情已出，映射完成，因子算完 → 形成信号  
- **T+1 开盘**：在**已映射的真实合约**上开仓  
- 持有到 **T+6 开盘**（5 日标签）平仓，仍用**同一真实合约**的开盘价（固定合约持有，不中途再选主力）

这就是「研究连续、交易真实」的工程落地。

---

# 6. 为何期限结构不依赖主力映射；换月与板块

### 为何曲线独立于主力
- **主力映射**回答：今天交易哪一张合约  
- **期限结构**回答：今天近月/远月价格形状如何（Carry、斜率、曲率）

若曲线只拿主力一张，就没有「曲线」。  
所以从**全部真实合约**里，按到期远近与流动性门槛筛出近/中/远月，独立算斜率曲率。  
这样 Carry 类因子不会因为「主力碰巧是哪张」而扭曲整条曲线定义。

### 换月如何操作
面板上主力合约代码变化时记换月；收益上区分：

- 正常日收益：同一合约价格变化  
- 换月日：可能涉及旧合约→新合约的衔接（单独记录，避免混进普通成本）

标签侧更干净：**信号日锁定真实合约，持有期内不因主力切换而换合约**，避免换月伪收益进入主标签。

### 板块归属
不是每天动态聚类，而是 `project.json` 里**预注册静态映射**：

- energy / metals / chemicals / agriculture  
- 每个品种唯一归属  

用于板块稳健性热力图，不是交易信号本身。

---

# 7. 「标签是未来走完后可知」是什么意思

对信号日 \(T\)：

\[
y_T^{(5)}=\frac{\mathrm{Open}_{c,T+6}}{\mathrm{Open}_{c,T+1}}-1
\]

- **在历史回测里**：到了 \(T+6\) 之后，数据库里已有这些开盘价，所以程序能算出 \(y_T\)  
- **在真实交易的 \(T\) 日晚上**：\(T+1\)、\(T+6\) 还没发生，你**不可能知道** \(y_T\)

所以：

- 标签 = 批改用的标准答案（事后可知）  
- 因子/模型 = 考试时你能用的卷面信息（\(T\) 时可知）  
- 训练是用「历史题目+历史答案」学映射；上线时只有题目，没有答案

你缺的概念正是：**研究数据库的事后全知 ≠ 交易时点的信息集**。

---

# 8. 为什么要用三组 \(h\)（1/5/20）

不是三个交易策略并重，而是**稳健性探针**：

| 期限     | 角色                                       |
| -------- | ------------------------------------------ |
| 1 日     | 很短；若只有 1 日有效，可能是噪声/微观结构 |
| **5 日** | **主协议**（与 5 日调仓对齐）              |
| 20 日    | 更慢；看信号是否只是短期反转               |

若某因子 5 日很好、1/20 全崩，要警惕过拟合到主期限。  
主文结论仍以 5 日为准，1/20 是附录式压力测试。

---

# 9. 47 → 92 个 ML 字段：方法、逻辑、规则

### 为何不能直接把原始因子喂模型
1. 量纲不同（Carry 与波动率差几个数量级）  
2. 缺失普遍（库存覆盖低）  
3. 横截面排序交易关心的是**相对强弱**，不是绝对水平  
4. 共线存在，但不能用 y 来删特征（防泄露）

### 硬门（先决定谁能进）
可计算 + 非零方差 + 覆盖率/事件观测门槛 → 约 47 个通过；其余 F/质量门淘汰，**不进特征矩阵**。

### 加工方法与内在逻辑

**A. 横截面稳健标准化 `__csz`（大多数品种级因子）**  
同日 30 品种内：中位数/MAD → 类 z-score，截断（clip=5），缺失填当日中性（标准化后约 0）。  

逻辑：组合是横截面多空；模型应看「今天谁相对更强」，且不受极端值绑架；MAD 比标准差抗野值。

**B. 仅历史滚动标准化 `__tsz`（少数市场级/时序变量）**  
用过去窗口均值方差，**不含当天未来**。  

逻辑：全市场状态变量没有「横截面同伴」，只能对自己的历史标准化；必须 prior-only 防泄露。

**C. 二元变量**  
缺失填 0，保持 0/1。  

**D. 缺失指示 `__missing`**  
当原始缺失率高于阈值时，额外加一列 0/1。  

逻辑：有时「缺失本身」是信息（如库存不披露的品种），不能假装成中性值而不告诉模型。

**E. 相关簇元数据**  
算相关、分簇、标代表元，但 **`retained_despite_correlation=True`**：不物理删除。  

逻辑：删谁若依赖全样本相关或更糟依赖 y，会污染；留给 Ridge/Lasso/树自己收缩。

**F. 禁止用标签做特征选择**  
否则特征集合本身就偷看了答案。

### 47 → 92 怎么来的（直觉）
约 47 个通过硬门的概念因子，多数变成 `__csz`（或 `__tsz`），一部分再附 `__missing`，再加少量预注册交互/质量字段 → 注册表合计 **92** 个 `feature_name`。  
最终矩阵断言：**无 NaN**（全部填过），才能当稳定 ML 接口。

---

# 10. 评价维度的计算流程（按程序步骤，不背公式）

对每个因子，在开发评价期（约 2019–2024）大致走：

### A. 日度横截面预测力（IC 路径）
1. 取当日有因子且有 `future_return_5d` 的品种（截面人数够才算）  
2. 算当日 Pearson IC、Rank IC（因子排序 vs 未来收益排序）  
3. 得到一条按交易日排列的 IC 时间序列  
4. 对序列求均值 → `ic_mean` / `rank_ic`  
5. 求标准差 → IR（均值/标准差）  
6. 命中率：Rank IC>0 的日子占比  
7. HAC 检验：IC 序列均值是否显著异于 0（考虑自相关）  
8. 全库多个因子的 p 值做 BH-FDR → `rank_ic_fdr_q`

### B. 组合路径（可交易性）
1. 每 5 个交易日取一个调仓日  
2. 当日按因子取向排序  
3. 前 20% 等权多、后 20% 等权空（多头总权 +0.5，空头 −0.5）  
4. 组合毛收益 = Σ 权重 × 该品种 `future_return_5d`  
5. 换手 = 相对上一期权重变化绝对值之和  
6. 净收益 = 毛收益 − 换手×成本bps  
7. 对净收益序列算年化、波动、Sharpe、最大回撤等  
8. 再对组合收益做 HAC；全库 FDR → `portfolio_fdr_q`

### C. 分位单调性
同调仓日把品种切成 Q1–Q5，各组平均 `future_return_5d`，看是否随分位递增；与分位序号相关得 `monotonicity`。

### D. 稳健性支线（多套重复 A/B 的变体）
- 换标签 1日/20日再算 IC  
- 信号整体延迟 1 日再算  
- 按年切开看每年 Rank IC  
- 按品种做时间序列相关  
- 按板块截面 IC  
- 邻域参数重算（诊断）  
- Ridge 增量：在固定基线特征上加该因子，看 OOS ΔRankIC/ΔMSE

### E. 汇总分类
把上述证据塞进规则，输出 A–F（本轮 A=0）。

这就是「很多」的来源：**同一套因果故事，在很多切片上重复跑**。

---

# 11. 为何 `training_handoff` 能当下游唯一接口

### 它保证什么
交接包是一份**合同化数据包**，不是随手导出：

| 文件                            | 合同内容                                     |
| ------------------------------- | -------------------------------------------- |
| `ml_feature_registry.csv`       | 合法输入字段全集（顺序/名称）                |
| `ml_dataset_development.csv.gz` | 按合同列对齐的 X+y（本地有；git 不传大文件） |
| `fold_definitions.csv`          | 唯一合法时间切分                             |
| `training_contract.json`        | 标签名、执行修订、禁止事项                   |
| 协议快照 / SHA256               | 版本钉死                                     |

下游只要：

```text
读 registry 的 feature_name
→ 在 dataset 里取这些列当 X
→ 取 future_return_5d 当 y
→ 按 fold_definitions 训练
→ 输出 trade_date×product 的预测值
→ 再用同一执行协议做组合
```

就与上游对齐。

### 「信息损失」如何理解
- **不会丢**：通过硬门的可计算因子都进了 92 维接口；相关簇只标注不删除  
- **会变换**：原始量纲被标准化、缺失被填充——这是有意的表示学习前处理，不是随机丢掉经济含义  
- **刻意不传**：2025、以及未过硬门的 F 类原始空列（避免假装有数据）

### 规则有没有传到下游
有，但是以**文件形式冻结**，不是口头：

- 扩展折、主标签、T+1 开盘、5bps、禁止随机切分、禁止看 2025  
都写在 `training_contract.json` / `PROTOCOL_AMENDMENT.md` / `fold_definitions.csv`  

下游若不遵守，是违约，不是接口没传。

---

# 12. 两句防泄露话的含义 + 为什么要防泄露

### 「2025 仅 sealed_evaluation，永不进 training_handoff」
建模同学的文件夹里**物理上没有 2025 行**。  
即使有人想偷看，默认路径也拿不到。这是用文件隔离落实「期末考密封」。

### 「基本面观察日 → +1 交易日才进信号」
假设库存观察日是周一。若周一收盘就用它算因子，但现实中你可能要到周二才稳定可见（且本数据无日内时点）。  
于是规定：观察日之后再等 **1 个交易日** 才允许进入特征。  

更严：现货太旧（>7 天）直接不用，宁可缺失。

### 为什么防泄露是灵魂
量化回测最大的假阳性来源不是模型不够复杂，而是：

> 用了**当时不可能知道**的信息，把未来答案漏进了特征或样本选择。

泄露会让 IC、Sharpe 好看，一上真实时序就塌。  
你项目里所有「看起来烦人」的规则（拒绝覆盖、封存、PIT、+1 日、不用 y 选特征、固定执行）都在打同一个敌人：**虚假的样本外**。

---

## 建议你建立的心智模型（补齐缺口）

```text
信息集 I_T（T日收盘可知）──► 因子/特征 X_T ──► 模型预测 ŷ_T
                                              │
未来行情（T之后）──────────────────────────► 标签 y_T（仅事后评分）

开发期：用历史 (X,y) 学 f，并用扩展窗口做“模拟OOS”
冻结后：f 与规则锁定，打开2025密封袋，只考一次
```

若只记一句：

> **扩展窗口是练兵，封存 2025 是决战；标签是事后答案，特征只能用当时信息集；交接包是 API 合同，不是数据包搬运。**

你若愿意，下一步我可以用「某一天某一个品种（例如 2020-06-30 的 CU）」把：映射 → 因子 → 标签 → 缺失标准化 → 调仓权重，按真实表字段走一遍数字例子，把抽象全部钉死。